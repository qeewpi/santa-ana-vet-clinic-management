export async function handleView(id) {
  // Alert the id instead of returning a JSX element
  alert(`View client with id: ${id}`);
}

export async function handleEdit(id) {
  // Alert the id instead of returning a JSX element
  alert(`Edit client with id: ${id}`);
}
